# 88. Merge Sorted Array

### Problem
You are given two integer arrays nums1 and nums2, sorted in non-decreasing order, and two integers m and n, representing the number of elements in nums1 and nums2 respectively.

Merge nums1 and nums2 into a single array sorted in non-decreasing order.

The final sorted array should not be returned by the function, but instead be stored inside the array nums1. To accommodate this, nums1 has a length of m + n, where the first m elements denote the elements that should be merged, and the last n elements are set to 0 and should be ignored. nums2 has a length of n.
```
Example 1:
Input: nums1 = [1,2,3,0,0,0], m = 3, nums2 = [2,5,6], n = 3
Output: [1,2,2,3,5,6]
Explanation: The arrays we are merging are [1,2,3] and [2,5,6].
The result of the merge is [1,2,2,3,5,6] with the underlined elements coming from nums1.
```
### Note
```
Avoid writing to and reading from nums1 simultaneously, as improper handling could overwrite the original data.
Array index boundary check.

# code sample
nums1[tail--] = nums2[idx2--];
// Equivalent to:
nums1[tail] = nums2[idx2];
tail--;
idx2--;
```
### Solution 1
```c =
void merge(int* nums1, int nums1Size, int m, int* nums2, int nums2Size, int n) {

    while ((0<m) && (0<n)){

        if (nums1[m-1]<=nums2[n-1]){
            nums1[m+n-1] = nums2[n-1];
            n--;
        }
        else{
            nums1[m+n-1] = nums1[m-1];
            m--;
        }
    }

    while (0<n){
        nums1[m+n-1] = nums2[n-1];
        n--;
    }

}
```
### Issue 1 & Fix 1
```
The code is not readable.
```

### Solution 2
```c =
void merge(int* nums1, int nums1Size, int m, int* nums2, int nums2Size, int n) {

    int idx1 = m - 1;        // nums1 last valid element
    int idx2 = n - 1;        // nums2 last element
    int tail = m + n - 1;    // nums1 index tail

    while (idx1 >= 0 && idx2 >= 0){

        if (nums1[idx1] > nums2[idx2]){
            nums1[tail--] = nums1[idx1--];
        } 
        else{
            nums1[tail--] = nums2[idx2--];
        }
    }

    // There will be elements left over from either num1 or num2.
    // Since num1 already contains the array that needs to be returned, we only need to move num2 into num1.
    while (idx2 >= 0){
        nums1[tail--] = nums2[idx2--];
    }

}
```
